#include <bits/stdc++.h>
using namespace std;
/*
1. union_find 해결
2. 최소의 간선의 개수는 노드 -1
*/



int main()
{
	ios::sync_with_stdio(0);
	cin.tie(0);
	int t;
	cin >> t;
	while (t--)
	{
		int n, m;
		cin >> n >> m;
		int a, b;
		while (m--)
		{
			cin >> a >> b;
		}
		cout << n - 1 << '\n';
	}
}